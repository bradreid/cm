%div.form
  %h2=t :title, :scope=>[:admin,:newtool]
  - form_for [:admin, @tool], :builder => CmFormBuilder, :html => { :multipart => true }  do |f|
    = f.error_messages
    = render :partial => "form", :object => f
    %br
    = submit_tag t(:button_new,:scope=>[:admin,:newtool]), :class => "btn primary"
    
    = link_to "Cancel", admin_tools_path, :class => "btn danger"
%br
%br
