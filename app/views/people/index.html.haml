.clear_both
.page-header
  %h1
    =t :title, :scope => [:search, :people]
    %small
      =t :subtitle, :scope => [:search, :people]
      
.left
  =render :partial => 'shared/search', :locals => {:path => people_path}
=render :partial => 'shared/tic_search_results'    
